/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  output: 'export' // This enables static site export (next export)
}

module.exports = nextConfig
